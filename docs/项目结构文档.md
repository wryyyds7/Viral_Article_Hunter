# 爆文猎人 — 项目结构文档

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-06-04 |
| 约束 | 后续开发严格按照本结构创建文件，除非新增功能模块，否则不得创建本文件未列出的文件 |

---

## 项目总览

```
Viral_Article_Hunter/
│
├── backend/                          # 后端 Python 项目
├── frontend/                         # 前端 React 项目
├── docs/                             # 项目文档
├── scripts/                          # 运维脚本
├── docker-compose.yml                # 容器编排
├── Dockerfile                        # 后端镜像构建
├── .env.example                      # 环境变量模板
├── .gitignore                        # Git 忽略规则
└── README.md                         # 项目说明
```

---

## 一、backend/ — 后端项目

```
backend/
│
├── app/                              # 应用主包
│   │
│   ├── __init__.py                   # 包初始化，版本号等元信息
│   │
│   ├── main.py                       # FastAPI 应用入口
│   │                                 # - 创建 FastAPI 实例
│   │                                 # - 注册所有路由(router)
│   │                                 # - 挂载中间件(CORS/Auth/Quota/RateLimit)
│   │                                 # - 注册 EventBus 事件处理器
│   │                                 # - 启动时初始化: DB连接池/APScheduler/首个admin检测
│   │                                 # - lifespan 管理: startup/shutdown 事件
│   │
│   ├── config.py                     # 全局配置（从环境变量读取）
│   │                                 # - DATABASE_URL / REDIS_URL
│   │                                 # - LLM_API_KEY / LLM_BASE_URL / LLM_MODEL
│   │                                 # - REDFOX_API_KEY / SEARCH_API_KEY
│   │                                 # - JWT_SECRET / JWT_ALGORITHM
│   │                                 # - ENCRYPTION_KEY (API Key加密密钥)
│   │                                 # - UPLOAD_THRESHOLD / MAX_FILE_SIZE_MB
│   │                                 # - CORS_ORIGINS / LOG_LEVEL
│   │                                 # 所有配置集中在此文件，其他模块 from app.config import settings
│   │
│   ├── database.py                   # 数据库连接与会话管理
│   │                                 # - create_async_engine(DATABASE_URL)
│   │                                 # - AsyncSessionLocal 会话工厂
│   │                                 # - get_db() 依赖注入生成器
│   │                                 # - Base = declarative_base()
│   │
│   ├── event_bus.py                  # 进程内事件总线
│   │                                 # - EventBus 类: on()/emit()/off()
│   │                                 # - 事件注册表: _handlers: Dict[str, List[Callable]]
│   │                                 # - emit() 异步执行所有处理器
│   │                                 # - 内置事件常量: COLLECT_COMPLETED, ANALYZE_COMPLETED 等
│   │
│   ├── exceptions.py                 # 自定义业务异常
│   │                                 # - AppException(基类)
│   │                                 # - QuotaExceededError
│   │                                 # - LLMServiceError
│   │                                 # - CollectionError
│   │                                 # - UploadParseError
│   │                                 # - InsufficientArticlesError(<10篇)
│   │                                 # - AdminPermissionError
│   │                                 # 统一异常处理器注册在 main.py
│   │
│   │
│   ├── models/                       # ORM 数据模型（每张表一个文件，与DDL一一对应）
│   │   │
│   │   ├── __init__.py               # 导出所有 Model，供 Alembic env.py 引用
│   │   │
│   │   ├── user.py                   # users 表 + refresh_tokens 表
│   │   │                             # - User: id/username/email/hashed_password/role/status/created_at/updated_at
│   │   │                             # - RefreshToken: id/user_id/token_hash/device_info/ip_address/expires_at/revoked_at
│   │   │
│   │   ├── article.py                # article 表
│   │   │                             # - Article: id/user_id/title/content/summary/source_url/source_platform/
│   │   │                             #   source_author/collection_method/upload_file_id/published_at/collected_at/
│   │   │                             #   is_favorited/tags/created_at/updated_at
│   │   │                             # - GIN索引: tags, title_trgm
│   │   │
│   │   ├── analysis.py               # analysis_result 表
│   │   │                             # - AnalysisResult: id/article_id/user_id/hotness_score(JSONB)/emotion_tags/
│   │   │                             #   title_formulas/structure_template/top_3_genes/platform_fit/
│   │   │                             #   viral_factors/improvement_suggestions/raw_llm_response/
│   │   │                             #   llm_model/llm_tokens_used/analyzed_at/created_at
│   │   │
│   │   ├── rewrite.py                # rewrite_task 表 + rewrite_result 表
│   │   │                             # - RewriteTask: id/article_id/user_id/target_platforms/style_overrides/
│   │   │                             #   status/created_at/updated_at
│   │   │                             # - RewriteResult: id/task_id/platform/title/content/similarity_score/
│   │   │                             #   word_count/llm_model/llm_tokens_used/status/error_message/created_at
│   │   │                             #   UNIQUE(task_id, platform)
│   │   │
│   │   ├── collection.py             # collection_task 表
│   │   │                             # - CollectionTask: id/user_id/keyword/platforms/max_results/status/
│   │   │                             #   collected_count/error_message/started_at/completed_at/created_at
│   │   │
│   │   ├── upload.py                 # upload_batch 表 + upload_file 表
│   │   │                             # - UploadBatch: id/user_id/total_count/success_count/fail_count/
│   │   │                             #   threshold_met/status/created_at/updated_at
│   │   │                             # - UploadFile: id/batch_id/original_name/file_format/file_size/status/
│   │   │                             #   parsed_title/article_id/error_message/created_at/updated_at
│   │   │
│   │   ├── favorite.py               # favorite_group 表 + favorite 表 (v1.1)
│   │   │                             # - FavoriteGroup: id/user_id/name/sort_order/created_at
│   │   │                             #   UNIQUE(user_id, name)
│   │   │                             # - Favorite: id/user_id/article_id/group_id/created_at
│   │   │                             #   UNIQUE(user_id, article_id, group_id)
│   │   │
│   │   ├── admin.py                  # admin_audit_log 表 + api_keys 表 + system_config 表
│   │   │                             # - AdminAuditLog: id/admin_id/action/target_type/target_id/detail/
│   │   │                             #   ip_address/created_at (不可UPDATE/DELETE)
│   │   │                             # - ApiKey: id/service/encrypted_key/key_hint/is_active/last_used_at/
│   │   │                             #   expires_at/created_by/created_at  UNIQUE(service, key_hint)
│   │   │                             # - SystemConfig: key(PK)/value(JSONB)/description/updated_by/updated_at
│   │   │
│   │   └── quota.py                  # user_quota 表
│   │                                 # - UserQuota: id/user_id(UNIQUE)/daily_llm_tokens/daily_rewrites/
│   │                                 #   daily_collections/daily_uploads/used_llm_tokens/used_rewrites/
│   │                                 #   used_collections/used_uploads/quota_reset_at/created_at/updated_at
│   │
│   │
│   ├── schemas/                      # Pydantic 请求/响应模型（与接口文档A01-A08对应）
│   │   │
│   │   ├── __init__.py               # 导出常用基类
│   │   │
│   │   ├── base.py                   # 公共基类
│   │   │                             # - ResponseBase[T]: {code, message, data}
│   │   │                             # - PaginationRequest: {page, page_size}
│   │   │                             # - PaginatedResponse[T]: {items, total, page, page_size}
│   │   │                             # - SortRequest: {sort_by, sort_order}
│   │   │
│   │   ├── auth.py                   # 认证相关 (对应 A00 登录注册)
│   │   │                             # - RegisterRequest: {email, password, username?}
│   │   │                             # - LoginRequest: {email, password}
│   │   │                             # - TokenResponse: {access_token, token_type, user}
│   │   │                             # - RefreshResponse: {access_token}
│   │   │                             # - UserBrief: {id, username, email, role, status}
│   │   │
│   │   ├── collection.py             # 采集相关 (对应 A01)
│   │   │                             # - CollectRequest: {keyword, platforms, max_results}
│   │   │                             # - CollectTaskResponse: {task_id, status, ...}
│   │   │                             # - CollectResultItem: {article_id, title, platform, ...}
│   │   │                             # - CollectListResponse: PaginatedResponse[CollectResultItem]
│   │   │
│   │   ├── analysis.py               # 分析相关 (对应 A02)
│   │   │                             # - AnalysisRequest: {article_id}
│   │   │                             # - BatchAnalysisRequest: {article_ids}
│   │   │                             # - AnalysisResponse: {id, hotness_score, emotion_tags, ...}
│   │   │                             # - TopGene: {rank, gene, reason}
│   │   │                             # - PlatformFit: {platform, fit_score}
│   │   │
│   │   ├── rewrite.py                # 改写相关 (对应 A03)
│   │   │                             # - RewriteRequest: {article_id, target_platforms, style_overrides?}
│   │   │                             # - StyleOverrides: {tone?, emoji_density?, emotion_intensity?, length?}
│   │   │                             # - RewriteTaskResponse: {task_id, status, ...}
│   │   │                             # - RewriteResultItem: {platform, title, content, similarity_score, ...}
│   │   │                             # - SSEEvent: {event_type, platform?, data}
│   │   │
│   │   ├── upload.py                 # 上传相关 (对应 A07)
│   │   │                             # - UploadBatchResponse: {batch_id, total_count, ...}
│   │   │                             # - UploadFileItem: {file_id, original_name, status, ...}
│   │   │                             # - RetryRequest: {file_ids}
│   │   │                             # - AnalyzeTriggerRequest: {confirmed: true}
│   │   │
│   │   ├── library.py                # 素材库相关 (对应 A04)
│   │   │                             # - LibraryQuery: {keyword?, platform?, tags?, is_favorited?, page, page_size}
│   │   │                             # - ArticleDetail: {id, title, content, platform, tags, analysis?, ...}
│   │   │                             # - FavoriteRequest: {article_id, group_id?}
│   │   │                             # - BatchActionRequest: {article_ids, action}
│   │   │
│   │   ├── dashboard.py              # 看板相关 (对应 A05)
│   │   │                             # - DashboardOverview: {today_collected, hot_articles, pending_analysis, ...}
│   │   │                             # - TrendData: {dates, counts, platforms}
│   │   │                             # - PlatformCompare: {platform, count, avg_score, ...}
│   │   │                             # - DashboardRequest: {range, start_date?, end_date?}
│   │   │
│   │   ├── settings.py               # 设置相关 (对应 A06)
│   │   │                             # - ApiKeyCreate: {service, key}
│   │   │                             # - CollectPlanCreate: {keyword, platforms, cron_expr}
│   │   │                             # - RewritePreference: {tone, emoji_density, emotion_intensity, length}
│   │   │
│   │   └── admin.py                  # 管理后台相关 (对应 A08)
│   │                                 # - AdminUserList: PaginatedResponse[UserDetail]
│   │                                 # - UserActionRequest: {user_id, action, reason?}
│   │                                 # - QuotaAdjustRequest: {user_id, daily_rewrites?, daily_analyses?, ...}
│   │                                 # - ApiKeyManageRequest: {service, key, expires_at?}
│   │                                 # - SystemConfigUpdate: {key, value}
│   │                                 # - AuditLogQuery: {admin_id?, action?, start_date?, end_date?, page, page_size}
│   │
│   │
│   ├── routers/                      # API 路由层（薄层，只做参数校验+调用service+返回响应）
│   │   │
│   │   ├── __init__.py               # 汇总所有 router 供 main.py 注册
│   │   │
│   │   ├── auth.py                   # /v1/auth/*
│   │   │                             # POST   /v1/auth/register     → auth_service.register()
│   │   │                             # POST   /v1/auth/login        → auth_service.login()
│   │   │                             # POST   /v1/auth/refresh      → auth_service.refresh()
│   │   │                             # POST   /v1/auth/logout       → auth_service.logout()
│   │   │                             # GET    /v1/auth/me           → auth_service.get_current_user_info()
│   │   │
│   │   ├── collection.py             # /v1/collect/*
│   │   │                             # POST   /v1/collect           → collect_service.create_task()
│   │   │                             # GET    /v1/collect/tasks      → collect_service.list_tasks()
│   │   │                             # GET    /v1/collect/tasks/{id} → collect_service.get_task()
│   │   │                             # GET    /v1/collect/results    → collect_service.list_results()
│   │   │                             # POST   /v1/collect/tasks/{id}/retry → collect_service.retry()
│   │   │
│   │   ├── analysis.py               # /v1/analysis/*
│   │   │                             # POST   /v1/analysis/{article_id}       → analysis_service.analyze()
│   │   │                             # POST   /v1/analysis/batch              → analysis_service.batch_analyze()
│   │   │                             # GET    /v1/analysis/{article_id}/result → analysis_service.get_result()
│   │   │                             # GET    /v1/analysis/results             → analysis_service.list_results()
│   │   │
│   │   ├── rewrite.py                # /v1/rewrite/*
│   │   │                             # POST   /v1/rewrite                      → rewrite_service.create_task()
│   │   │                             # GET    /v1/rewrite/tasks                 → rewrite_service.list_tasks()
│   │   │                             # GET    /v1/rewrite/tasks/{id}            → rewrite_service.get_task()
│   │   │                             # GET    /v1/rewrite/tasks/{id}/results    → rewrite_service.get_results()
│   │   │                             # GET    /v1/rewrite/tasks/{id}/stream     → rewrite_service.sse_stream()
│   │   │                             # POST   /v1/rewrite/tasks/{id}/retry      → rewrite_service.retry()
│   │   │
│   │   ├── upload.py                 # /v1/upload/*
│   │   │                             # POST   /v1/upload/batch                    → upload_service.create_batch()
│   │   │                             # GET    /v1/upload/batches/{id}             → upload_service.get_batch()
│   │   │                             # POST   /v1/upload/batches/{id}/retry       → upload_service.retry_failed()
│   │   │                             # POST   /v1/upload/batches/{id}/analyze     → upload_service.trigger_analysis()
│   │   │                             # GET    /v1/upload/batches/{id}/progress    → upload_service.get_progress()
│   │   │
│   │   ├── library.py                # /v1/library/*
│   │   │                             # GET    /v1/library/articles              → library_service.list_articles()
│   │   │                             # GET    /v1/library/articles/{id}          → library_service.get_article()
│   │   │                             # DELETE /v1/library/articles/{id}          → library_service.delete_article()
│   │   │                             # POST   /v1/library/articles/{id}/favorite → library_service.toggle_favorite()
│   │   │                             # POST   /v1/library/articles/batch         → library_service.batch_action()
│   │   │                             # GET    /v1/library/tags                   → library_service.list_tags()
│   │   │                             # POST   /v1/library/export                 → library_service.export()
│   │   │
│   │   ├── dashboard.py              # /v1/dashboard/*
│   │   │                             # GET    /v1/dashboard/overview            → dashboard_service.get_overview()
│   │   │                             # GET    /v1/dashboard/trends              → dashboard_service.get_trends()
│   │   │                             # GET    /v1/dashboard/platform-compare    → dashboard_service.get_platform_compare()
│   │   │                             # GET    /v1/dashboard/stats               → dashboard_service.get_stats()
│   │   │
│   │   ├── settings.py               # /v1/settings/*
│   │   │                             # GET    /v1/settings/api-keys             → settings_service.list_api_keys()
│   │   │                             # POST   /v1/settings/api-keys             → settings_service.create_api_key()
│   │   │                             # DELETE /v1/settings/api-keys/{id}        → settings_service.delete_api_key()
│   │   │                             # GET    /v1/settings/collect-plans         → settings_service.list_plans()
│   │   │                             # POST   /v1/settings/collect-plans         → settings_service.create_plan()
│   │   │                             # PUT    /v1/settings/collect-plans/{id}    → settings_service.update_plan()
│   │   │                             # DELETE /v1/settings/collect-plans/{id}    → settings_service.delete_plan()
│   │   │                             # GET    /v1/settings/rewrite-preference    → settings_service.get_preference()
│   │   │                             # PUT    /v1/settings/rewrite-preference    → settings_service.update_preference()
│   │   │
│   │   └── admin.py                  # /v1/admin/*  (需 Depends(admin_only))
│   │                                 # GET    /v1/admin/users                  → admin_service.list_users()
│   │                                 # GET    /v1/admin/users/{id}             → admin_service.get_user()
│   │                                 # POST   /v1/admin/users/{id}/ban        → admin_service.ban_user()
│   │                                 # POST   /v1/admin/users/{id}/unban      → admin_service.unban_user()
│   │                                 # PUT    /v1/admin/users/{id}/role        → admin_service.change_role()
│   │                                 # PUT    /v1/admin/users/{id}/quota       → admin_service.adjust_quota()
│   │                                 # GET    /v1/admin/monitor                → admin_service.get_monitor()
│   │                                 # GET    /v1/admin/api-keys               → admin_service.list_api_keys()
│   │                                 # POST   /v1/admin/api-keys               → admin_service.create_api_key()
│   │                                 # DELETE /v1/admin/api-keys/{id}          → admin_service.revoke_api_key()
│   │                                 # GET    /v1/admin/content/flagged        → admin_service.list_flagged()
│   │                                 # POST   /v1/admin/content/{id}/action    → admin_service.content_action()
│   │                                 # GET    /v1/admin/config                 → admin_service.list_config()
│   │                                 # PUT    /v1/admin/config/{key}           → admin_service.update_config()
│   │                                 # GET    /v1/admin/audit-logs             → admin_service.list_audit_logs()
│   │
│   │
│   ├── services/                     # 业务逻辑层（核心，router调service，service调model/util/collector）
│   │   │
│   │   ├── __init__.py
│   │   │
│   │   ├── auth_service.py           # 认证业务
│   │   │                             # - register(): 创建用户 + 首用户admin + 初始化quota
│   │   │                             # - login(): 验证密码 + 生成Token + 登录失败计数
│   │   │                             # - refresh(): 验证RefreshToken + 生成新Token + 撤销旧Token
│   │   │                             # - logout(): 撤销RefreshToken
│   │   │                             # - get_current_user_info(): 返回用户详情+配额用量
│   │   │
│   │   ├── collect_service.py        # 采集业务
│   │   │                             # - create_task(): 创建任务 + 调度器入队
│   │   │                             # - execute_task(): 调用collector + 批量创建article + 发事件
│   │   │                             # - list_tasks(): 查询用户采集任务列表
│   │   │                             # - get_task(): 查询单个任务详情
│   │   │                             # - list_results(): 查询采集到的文章(共享+私有)
│   │   │                             # - retry(): 重试失败任务
│   │   │
│   │   ├── upload_service.py         # 文档上传业务
│   │   │                             # - create_batch(): 创建批次 + 逐文件分发解析
│   │   │                             # - parse_file(): 根据格式选择parser + 创建article
│   │   │                             # - check_threshold(): 判断是否≥10篇 + 决定是否自动分析
│   │   │                             # - get_batch(): 查询批次详情
│   │   │                             # - retry_failed(): 重新解析失败文件
│   │   │                             # - trigger_analysis(): 手动触发分析(阈值不足时用户确认)
│   │   │                             # - get_progress(): 查询批次进度
│   │   │
│   │   ├── analysis_service.py       # 爆款分析业务
│   │   │                             # - analyze(): 单篇分析 → LLM调用 → 适配层 → 写入结果
│   │   │                             # - batch_analyze(): 批量分析 → 逐篇执行 → 汇总通知
│   │   │                             # - auto_analyze(): 事件触发(采集/上传完成后自动调用)
│   │   │                             # - get_result(): 查询分析结果
│   │   │                             # - list_results(): 查询分析结果列表
│   │   │
│   │   ├── rewrite_service.py        # 跨平台改写业务
│   │   │                             # - create_task(): 创建任务 + 多平台并行调度
│   │   │                             # - execute_platform_rewrite(): 单平台改写(被并行调用)
│   │   │                             # - quality_check(): 改写质量自检(相似度+平台风格)
│   │   │                             # - sse_stream(): SSE逐平台推送
│   │   │                             # - list_tasks(): 查询改写任务列表
│   │   │                             # - get_task(): 查询单个任务详情+结果
│   │   │                             # - retry(): 重试失败平台
│   │   │
│   │   ├── library_service.py        # 素材库业务
│   │   │                             # - list_articles(): 搜索+筛选+排序+分页
│   │   │                             # - get_article(): 单篇详情(含分析结果+改写记录)
│   │   │                             # - delete_article(): 软删除
│   │   │                             # - toggle_favorite(): 收藏/取消收藏
│   │   │                             # - batch_action(): 批量分析/删除/导出
│   │   │                             # - list_tags(): 标签云数据
│   │   │                             # - export(): 导出MD/Word/ZIP
│   │   │
│   │   ├── dashboard_service.py      # 数据看板业务
│   │   │                             # - get_overview(): 今日概览(采集数/热点数/待分析数)
│   │   │                             # - get_trends(): 趋势数据(7天/30天/90天)
│   │   │                             # - get_platform_compare(): 各平台对比
│   │   │                             # - get_stats(): 创作统计+Token消耗
│   │   │
│   │   ├── settings_service.py       # 设置业务
│   │   │                             # - list/create/delete_api_key(): API Key管理
│   │   │                             # - list/create/update/delete_plan(): 采集计划CRUD
│   │   │                             # - get/update_preference(): 改写偏好读写
│   │   │
│   │   └── admin_service.py          # 管理后台业务(所有操作都写审计日志)
│   │                                 # - list/get_user(): 用户查询
│   │                                 # - ban/unban_user(): 封禁/解封
│   │                                 # - change_role(): 角色变更
│   │                                 # - adjust_quota(): 配额调整
│   │                                 # - get_monitor(): 系统监控数据
│   │                                 # - list/create/revoke_api_key(): 全局Key管理
│   │                                 # - list_flagged()/content_action(): 内容审核
│   │                                 # - list/update_config(): 系统配置
│   │                                 # - list_audit_logs(): 审计日志查询
│   │                                 # - _write_audit_log(): 内部方法，写admin_audit_log
│   │
│   │
│   ├── collectors/                   # 采集器（对接外部API）
│   │   │
│   │   ├── __init__.py
│   │   │
│   │   ├── base_collector.py         # 采集器抽象基类
│   │   │                             # - BaseCollector(ABC)
│   │   │                             #   - collect(keyword, max_results) → List[ArticleData]
│   │   │                             #   - _normalize(raw_data) → ArticleData  (子类实现)
│   │   │                             #   - _handle_error(error) → 降级/重试逻辑
│   │   │                             # - ArticleData: {title, content, url, platform, author, published_at, metrics}
│   │   │
│   │   ├── honghu_collector.py       # 红狐数据采集器
│   │   │                             # - HonghuCollector(BaseCollector)
│   │   │                             # - 对接红狐API，采集小红书+公众号数据
│   │   │                             # - 支持关键词搜索 + 热榜采集
│   │   │                             # - 限流: 遵守红狐API调用频率
│   │   │                             # - 降级: 超时10秒 → 切search_collector
│   │   │
│   │   └── search_collector.py       # 联网搜索采集器
│   │                                 # - SearchCollector(BaseCollector)
│   │                                 # - 对接搜索API，采集头条/知乎/B站数据
│   │                                 # - 搜索结果解析 + 结构化
│   │                                 # - 作为L2备用源
│   │
│   │
│   ├── parsers/                      # 文档解析器（用户上传文档处理）
│   │   │
│   │   ├── __init__.py               # 工厂方法: get_parser(file_format) → BaseParser子类
│   │   │
│   │   ├── base_parser.py            # 解析器抽象基类
│   │   │                             # - BaseParser(ABC)
│   │   │                             #   - parse(file_path) → ParsedDocument
│   │   │                             #   - _detect_encoding(raw_bytes) → str  (chardet)
│   │   │                             # - ParsedDocument: {title, content, word_count, format}
│   │   │
│   │   ├── txt_parser.py             # TXT/MD 解析器
│   │   │                             # - TxtParser(BaseParser)
│   │   │                             # - chardet检测编码 → 读取全文
│   │   │                             # - MD文件: 提取第一个#标题作为title
│   │   │                             # - TXT文件: 取首行非空行作为title
│   │   │
│   │   ├── docx_parser.py            # DOCX 解析器
│   │   │                             # - DocxParser(BaseParser)
│   │   │                             # - python-docx 读取段落
│   │   │                             # - 提取第一个段落作为title，其余为content
│   │   │
│   │   ├── pdf_parser.py             # PDF 解析器
│   │   │                             # - PdfParser(BaseParser)
│   │   │                             # - pdfplumber 提取文本（优先）
│   │   │                             # - PyPDF2 作为备选
│   │   │                             # - 处理多栏/表格等复杂布局
│   │   │
│   │   └── html_parser.py            # HTML 解析器
│   │                                 # - HtmlParser(BaseParser)
│   │                                 # - BeautifulSoup 去除标签 + 提取纯文本
│   │                                 # - 提取 <title> 标签作为title
│   │                                 # - 去除 script/style/nav 等噪声
│   │
│   │
│   ├── middleware/                    # 中间件（请求拦截）
│   │   │
│   │   ├── __init__.py
│   │   │
│   │   ├── auth.py                   # 认证中间件
│   │   │                             # - get_current_user(): 从Bearer Token解析用户 (Depends注入)
│   │   │                             # - admin_only(): 校验 user.role == 'admin'
│   │   │                             #   → 否则抛出 AdminPermissionError(40301)
│   │   │
│   │   ├── quota.py                  # 配额检查中间件
│   │   │                             # - check_quota(resource: str): 检查用户当日配额
│   │   │                             #   - resource: 'analysis' | 'rewrite' | 'collection' | 'upload'
│   │   │                             #   - 超限 → QuotaExceededError(429)
│   │   │                             #   - 通过 → used_xxx += 1
│   │   │                             # - reset_if_needed(): 跨日自动重置配额
│   │   │
│   │   └── rate_limit.py             # 全局限流中间件
│   │                                 # - RateLimiter: 基于IP+路径的滑动窗口限流
│   │                                 # - 采集: 5次/分钟, 上传: 5次/分钟, 分析: 10次/分钟
│   │                                 # - 改写: 3次/分钟, 管理后台: 30次/分钟
│   │                                 # - 超限 → 429 + Retry-After 头
│   │
│   │
│   ├── utils/                        # 工具函数（纯逻辑，无业务状态）
│   │   │
│   │   ├── __init__.py
│   │   │
│   │   ├── crypto.py                 # 加解密工具
│   │   │                             # - encrypt_api_key(plain_key) → encrypted_str  (AES-256-GCM)
│   │   │                             # - decrypt_api_key(encrypted_str) → plain_key
│   │   │                             # - mask_key(plain_key) → key_hint ("sk-...3xYp")
│   │   │                             # - ENCRYPTION_KEY 从 config.py 读取
│   │   │
│   │   ├── http_client.py            # 统一 HTTP 客户端
│   │   │                             # - AsyncHttpClient: 封装 httpx.AsyncClient
│   │   │                             # - 自动重试(2次, 指数退避)
│   │   │                             # - 超时配置(连接5s, 读取30s)
│   │   │                             # - 请求日志(方法/URL/耗时/状态码)
│   │   │
│   │   ├── text_processor.py         # 文本处理工具
│   │   │                             # - compute_similarity(text_a, text_b) → float  (余弦相似度)
│   │   │                             # - extract_tags(text) → List[str]  (jieba分词+关键词提取)
│   │   │                             # - detect_sensitive_words(text) → List[str]  (敏感词匹配)
│   │   │                             # - truncate_text(text, max_length) → str
│   │   │
│   │   ├── exporter.py               # 导出工具
│   │   │                             # - export_as_md(article, analysis, rewrite_results) → str
│   │   │                             # - export_as_docx(content, title) → BytesIO
│   │   │                             # - export_as_zip(files: List[FileInfo]) → BytesIO
│   │   │
│   │   └── pagination.py             # 分页工具
│   │                                 # - paginate(query, params) → PaginatedResponse
│   │                                 # - apply_sorting(query, sort_params) → query
│   │                                 # - apply_filters(query, filters) → query
│   │
│   │
│   ├── llm/                          # LLM 调用与 Prompt 管理
│   │   │
│   │   ├── __init__.py
│   │   │
│   │   ├── client.py                 # LLM 客户端封装
│   │   │                             # - LLMClient: 封装 openai SDK
│   │   │                             # - chat_completion(messages, model, temperature) → str
│   │   │                             # - chat_completion_stream(messages) → AsyncGenerator[str]
│   │   │                             # - 自动重试(1次, 非Token超限时)
│   │   │                             # - Token计数 + 配额扣减
│   │   │
│   │   ├── adapter.py                # LLM 输出适配层（4步清洗）
│   │   │                             # - adapt_analysis_output(raw_json) → AnalysisResult
│   │   │                             # - adapt_rewrite_output(raw_json, platform) → RewriteResult
│   │   │                             # - 步骤1: 格式清洗(去除Markdown包裹/修复JSON语法)
│   │   │                             # - 步骤2: Schema校验(必填字段存在性+类型检查)
│   │   │                             # - 步骤3: 字段映射(Prompt字段名→接口字段名, 19个映射)
│   │   │                             # - 步骤4: 业务校验(评分范围/相似度阈值/枚举值合法性)
│   │   │                             # - 兜底策略: 字段缺失→默认值, 类型错误→强转, 全失败→规则引擎
│   │   │
│   │   └── prompts/                  # Prompt 模板（五层结构: Role/Skills/Rules/Workflow/OutputFormat）
│   │       │
│   │       ├── __init__.py           # 导出所有 Prompt 构建函数
│   │       │
│   │       ├── analysis_prompt.py    # 爆款基因分析 Prompt
│   │       │                         # - build_analysis_prompt(title, content, platform?) → str
│   │       │                         # - 包含6大维度: DAIG/情绪鸡尾酒/标题公式/结构模板/互动设计/平台适配
│   │       │                         # - JSON Schema 输出约束(与A02接口字段完全对齐)
│   │       │                         # - 批量版: build_batch_analysis_prompt(articles) → str
│   │       │
│   │       ├── rewrite_prompt.py     # 跨平台改写 Prompt
│   │       │                         # - build_rewrite_prompt(article, target_platform, style_overrides, platform_rules) → str
│   │       │                         # - 运行时注入平台规则(data/platform_rules/xxx.md)
│   │       │                         # - user_params映射: tone/emoji_density/emotion_intensity/length → Prompt文本
│   │       │
│   │       ├── quality_check_prompt.py # 改写质量自检 Prompt
│   │       │                         # - build_quality_check_prompt(original, rewrite, platform) → str
│   │       │                         # - 4维校验: 平台风格/相似度/完整性/可读性
│   │       │                         # - 输出: {pass: bool, issues: [], suggestions: []}
│   │       │
│   │       └── templates/            # Prompt 版本化存储目录
│   │                                 # - analysis_v1.0.json
│   │                                 # - rewrite_v1.0.json
│   │                                 # - quality_check_v1.0.json
│   │                                 # 每个JSON包含: version/role/skills/rules/workflow/output_format/created_at
│   │
│   │
│   └── scheduler/                    # 任务调度
│       │
│       ├── __init__.py
│       │
│       ├── task_queue.py             # 任务队列管理
│       │                             # - TaskQueue: 基于 asyncio.Queue 的优先级队列
│       │                             # - enqueue(task_type, task_id, priority)
│       │                             # - dequeue() → (task_type, task_id)
│       │                             # - 并发控制: collect=3, analyze=5, rewrite=7, parse=10
│       │
│       ├── worker.py                 # Worker 池
│       │                             # - Worker: 从队列取任务并执行
│       │                             # - 超时控制: collect=30s, analyze=60s, rewrite=90s, parse=30s
│       │                             # - 心跳上报: 每10秒更新任务状态
│       │
│       └── cron.py                   # 定时任务(采集计划)
│                                     # - APScheduler 集成
│                                     # - 注册定时采集计划
│                                     # - 暂停/恢复/删除计划
│
│
├── migrations/                       # Alembic 数据库迁移
│   ├── env.py                        # Alembic 环境配置(引用 app.models)
│   ├── script.py.mako                # 迁移脚本模板
│   └── versions/                     # 迁移版本文件
│       ├── 001_initial_tables.py     # 初始14张表 + 索引
│       └── ...                       # 后续增量迁移
│
├── data/                             # 静态数据文件
│   ├── sensitive_words.txt           # 敏感词库(每行一个词)
│   └── platform_rules/               # 平台改写规则知识库(被rewrite_prompt运行时注入)
│       ├── xiaohongshu.md            # 小红书: emoji密度高/标题党/种草风/短段落/标签#
│       ├── zhihu.md                  # 知乎: 专业严谨/长文/引用数据/逻辑清晰
│       ├── bilibili.md               # B站: 二次元梗/弹幕互动/标题夸张/分区特色
│       ├── douyin.md                 # 抖音: 口语化/节奏快/钩子前3秒/情绪化
│       ├── kuaishou.md               # 快手: 接地气/方言/生活化/真诚
│       ├── wechat.md                 # 公众号: 深度长文/权威/结构化/金句
│       └── wechat_video.md           # 视频号: 口播稿/节奏/互动引导/话题标签
│
├── tests/                            # 测试
│   ├── conftest.py                   # pytest fixtures: test_db, mock_llm, test_client
│   ├── unit/                         # 单元测试
│   │   ├── test_parsers.py           # 解析器测试(TXT/MD/DOCX/PDF/HTML各格式)
│   │   ├── test_adapter.py           # LLM适配层测试(格式修复/字段映射/兜底)
│   │   ├── test_text_processor.py    # 文本处理测试(相似度/分词/敏感词)
│   │   ├── test_crypto.py            # 加解密测试
│   │   ├── test_quota.py             # 配额逻辑测试(超限/重置)
│   │   ├── test_prompts.py           # Prompt生成测试(包含6维度/JSON格式)
│   │   └── test_auth.py              # 认证逻辑测试(密码哈希/Token生成/首用户admin)
│   ├── integration/                  # 集成测试
│   │   ├── test_collect_api.py       # 采集API端到端(Mock外部API)
│   │   ├── test_analysis_api.py      # 分析API端到端(Mock LLM)
│   │   ├── test_rewrite_api.py       # 改写API+SSE端到端
│   │   ├── test_upload_api.py        # 上传API端到端(实际文件)
│   │   ├── test_admin_api.py         # 管理后台API(权限+审计)
│   │   └── test_event_bus.py         # 事件总线集成测试
│   └── e2e/                          # E2E测试(Playwright)
│       ├── test_collect_analyze_rewrite.py  # 采集→分析→改写全链路
│       ├── test_upload_analyze_rewrite.py   # 上传→分析→改写全链路
│       └── test_admin_flow.py               # 管理员操作全链路
│
├── alembic.ini                       # Alembic 配置
├── requirements.txt                  # 生产依赖
├── requirements-dev.txt              # 开发依赖(pytest/ruff/httpx等)
└── .env.example                      # 环境变量模板(22个变量)
```

---

## 二、frontend/ — 前端项目

```
frontend/
│
├── src/
│   │
│   ├── api/                          # API 请求封装
│   │   ├── client.ts                 # Axios 实例(baseURL/拦截器/Token注入/刷新)
│   │   ├── auth.ts                   # 认证接口: register/login/refresh/logout/me
│   │   ├── collection.ts             # 采集接口: createTask/listTasks/listResults/retry
│   │   ├── analysis.ts               # 分析接口: analyze/batchAnalyze/getResult/listResults
│   │   ├── rewrite.ts                # 改写接口: createTask/listTasks/getResults + SSE流读取
│   │   ├── upload.ts                 # 上传接口: createBatch/getBatch/retryFailed/triggerAnalysis
│   │   ├── library.ts                # 素材库接口: listArticles/getArticle/favorite/batchAction/export
│   │   ├── dashboard.ts              # 看板接口: overview/trends/platformCompare/stats
│   │   ├── settings.ts               # 设置接口: apiKeys/collectPlans/rewritePreference
│   │   └── admin.ts                  # 管理后台接口: users/monitor/keys/content/config/auditLogs
│   │
│   ├── components/                   # 通用组件
│   │   ├── ui/                       # shadcn/ui 基础组件(Button/Input/Card/Dialog/Table等)
│   │   │
│   │   ├── layout/                   # 布局组件
│   │   │   ├── AppLayout.tsx         # 主布局(侧边栏+顶栏+内容区)
│   │   │   ├── Sidebar.tsx           # 侧边导航栏(功能菜单+用户信息)
│   │   │   └── Header.tsx            # 顶栏(搜索+通知+用户头像)
│   │   │
│   │   ├── common/                   # 业务通用组件
│   │   │   ├── ArticleCard.tsx       # 文章卡片(标题+摘要+平台+热度+操作按钮)
│   │   │   ├── AnalysisPanel.tsx     # 分析结果面板(6维度标签页)
│   │   │   ├── RewriteResultCard.tsx # 单平台改写结果卡片
│   │   │   ├── PlatformBadge.tsx     # 平台标识(小红书/知乎/...带颜色图标)
│   │   │   ├── HotnessBadge.tsx      # 热度等级徽章(S/A/B/C，带颜色)
│   │   │   ├── EmotionTag.tsx        # 情绪标签(带配色)
│   │   │   ├── ProgressTracker.tsx   # 任务进度追踪组件(采集/分析/改写/上传通用)
│   │   │   ├── SSEProvider.tsx       # SSE连接管理(改写流式结果消费)
│   │   │   ├── QuotaIndicator.tsx    # 配额用量指示器(进度条+数字)
│   │   │   └── EmptyState.tsx        # 空状态占位(图标+文案+操作按钮)
│   │   │
│   │   └── charts/                   # 图表组件(基于ECharts)
│   │       ├── TrendChart.tsx        # 趋势折线图
│   │       ├── PlatformRadar.tsx     # 平台适配雷达图
│   │       ├── EmotionCloud.tsx      # 情绪标签词云
│   │       └── PlatformBarChart.tsx  # 平台对比柱状图
│   │
│   ├── pages/                        # 页面组件(每个页面对应一个目录)
│   │   ├── auth/                     # 认证页面
│   │   │   ├── LoginPage.tsx         # 登录页
│   │   │   └── RegisterPage.tsx      # 注册页
│   │   │
│   │   ├── collection/               # 采集页面
│   │   │   └── CollectionPage.tsx    # 采集主页(搜索框+平台选择+结果列表+任务状态)
│   │   │
│   │   ├── upload/                   # 上传页面
│   │   │   └── UploadPage.tsx        # 上传主页(拖拽区+格式提示+批次列表+进度)
│   │   │
│   │   ├── analysis/                 # 分析页面
│   │   │   └── AnalysisPage.tsx      # 分析主页(选择素材+分析结果展示+批量操作)
│   │   │
│   │   ├── rewrite/                  # 改写页面
│   │   │   └── RewritePage.tsx       # 改写主页(选素材+选平台+调参+流式结果+对比视图)
│   │   │
│   │   ├── library/                  # 素材库页面
│   │   │   └── LibraryPage.tsx       # 素材库主页(搜索+筛选+列表+收藏+批量操作+导出)
│   │   │
│   │   ├── dashboard/                # 数据看板页面
│   │   │   └── DashboardPage.tsx     # 看板主页(概览卡片+趋势图+平台对比+创作统计)
│   │   │
│   │   ├── settings/                 # 设置页面
│   │   │   └── SettingsPage.tsx      # 设置主页(Tab切换: API Key/采集计划/改写偏好/数据管理)
│   │   │
│   │   └── admin/                    # 管理后台页面(仅admin可见)
│   │       └── AdminPage.tsx         # 管理主页(Tab切换: 用户/监控/Key/审核/配置/日志)
│   │
│   ├── stores/                       # Zustand 状态管理
│   │   ├── authStore.ts              # 认证状态(user/tokens/login/logout/refresh)
│   │   ├── articleStore.ts           # 文章/素材状态(列表/筛选/分页)
│   │   ├── taskStore.ts              # 任务状态(采集/分析/改写/上传任务列表与进度)
│   │   └── notificationStore.ts      # 通知状态(未读数/通知列表/标记已读)
│   │
│   ├── hooks/                        # 自定义 Hooks
│   │   ├── useAuth.ts                # 认证Hook(登录态/权限判断/admin判断)
│   │   ├── useSSE.ts                 # SSE Hook(连接/断开/消息处理/自动重连)
│   │   ├── usePagination.ts          # 分页Hook(页码/每页数/总数/翻页)
│   │   └── useQuota.ts               # 配额Hook(剩余配额/百分比/超限判断)
│   │
│   ├── types/                        # TypeScript 类型定义（与 schemas/ 对应）
│   │   ├── api.ts                    # API通用类型(ResponseBase/Pagination/ErrorCode)
│   │   ├── article.ts                # 文章类型(Article/ArticleDetail/ArticleQuery)
│   │   ├── analysis.ts               # 分析类型(AnalysisResult/HotnessScore/TopGene/PlatformFit)
│   │   ├── rewrite.ts                # 改写类型(RewriteTask/RewriteResult/StyleOverrides/SSEEvent)
│   │   ├── collection.ts             # 采集类型(CollectionTask/CollectRequest)
│   │   ├── upload.ts                 # 上传类型(UploadBatch/UploadFile)
│   │   ├── dashboard.ts              # 看板类型(Overview/TrendData/PlatformCompare)
│   │   ├── settings.ts               # 设置类型(ApiKey/CollectPlan/RewritePreference)
│   │   ├── admin.ts                  # 管理后台类型(AdminUser/AuditLog/SystemConfig)
│   │   └── user.ts                   # 用户类型(User/UserQuota/LoginRequest/RegisterRequest)
│   │
│   ├── utils/                        # 前端工具函数
│   │   ├── format.ts                 # 格式化(日期/数字/文件大小/平台名称映射)
│   │   ├── download.ts               # 文件下载(fetch+blob模式，非<a>download)
│   │   ├── platform.ts               # 平台相关(图标/颜色/名称/规则摘要)
│   │   └── constants.ts              # 常量(路由路径/错误码映射/默认值)
│   │
│   ├── styles/                       # 全局样式
│   │   └── globals.css               # Tailwind导入 + 自定义CSS变量 + 滚动条/动画
│   │
│   ├── App.tsx                       # 根组件(路由定义+全局Provider)
│   ├── main.tsx                      # 入口文件(ReactDOM渲染)
│   └── vite-env.d.ts                 # Vite类型声明
│
├── public/                           # 静态资源
│   └── favicon.ico                   # 网站图标
│
├── index.html                        # HTML入口
├── package.json                      # 依赖声明
├── tsconfig.json                     # TypeScript配置
├── vite.config.ts                    # Vite配置(代理/API路径)
├── tailwind.config.ts                # Tailwind配置(主题/颜色/字体)
└── postcss.config.js                 # PostCSS配置
```

---

## 三、docs/ — 项目文档

```
docs/
│
├── 产品文档/
│   └── 产品需求文档_PRD.md            # 产品需求权威来源
│
├── 功能文档/
│   ├── F01_多平台热点采集.md           # 采集功能设计
│   ├── F02_爆款基因分析.md            # 分析功能设计
│   ├── F03_一键跨平台改写.md           # 改写功能设计
│   ├── F04_素材库管理.md              # 素材库功能设计
│   ├── F05_数据看板.md               # 看板功能设计
│   ├── F06_设置.md                   # 设置功能设计
│   ├── F07_用户文档上传.md            # 上传功能设计
│   └── F08_管理后台.md               # 管理后台功能设计
│
├── 接口文档/
│   ├── A00_公共接口规范.md            # 错误码/枚举/分页/限流/字段校验
│   ├── A01_多平台热点采集.md           # 采集接口
│   ├── A02_爆款基因分析.md            # 分析接口
│   ├── A03_一键跨平台改写.md           # 改写接口(含SSE)
│   ├── A04_素材库管理.md              # 素材库接口
│   ├── A05_数据看板.md               # 看板接口
│   ├── A06_设置.md                   # 设置接口
│   ├── A07_用户文档上传.md            # 上传接口
│   └── A08_管理后台.md               # 管理后台接口
│
├── 技术文档/
│   ├── 技术方案与工程规范.md           # 技术栈/目录/数据库/事件/搜索/部署/测试(16章)
│   ├── 采集器与基础设施详细设计.md      # 采集器架构/解析器/阈值判断
│   ├── 认证与多用户架构设计.md         # 认证/鉴权/数据隔离/配额
│   ├── LLM_Prompt设计文档.md          # Prompt五层结构/适配层/版本管理
│   ├── LLM成本控制与安全管理.md        # Token预算/降级/安全审查
│   ├── 调度策略与任务编排.md           # 任务队列/状态机/降级/断点续传
│   └── 触发规则与通知机制.md           # 事件驱动/通知渠道
│
├── 功能流程文档.md                    # 本文档：全流程梳理
├── 项目结构文档.md                    # 本文档：文件结构与职责
├── Quick_Start.md                    # 快速上手指南
├── .env.example                      # 环境变量模板
└── CHANGELOG.md                      # API变更记录(§14版本兼容策略要求)
```

---

## 四、scripts/ — 运维脚本

```
scripts/
│
├── backup_pg.sh                      # PostgreSQL 每日全量备份(pg_dump + 保留30天 + rclone异地)
├── restore_pg.sh                     # 数据库恢复脚本
├── seed_admin.py                     # 手动创建admin用户(紧急恢复用)
└── health_check.sh                   # 健康检查(curl各服务端口 + 告警)
```

---

## 五、根目录文件

| 文件 | 作用 |
|------|------|
| `docker-compose.yml` | 容器编排: app + postgres + redis，开发/生产通用 |
| `Dockerfile` | 后端镜像: python:3.12-slim + pip install + alembic upgrade + uvicorn |
| `.env.example` | 22个环境变量模板，复制为 .env 后使用 |
| `.gitignore` | 忽略: __pycache__/.env/node_modules/.venv/*.pyc/dist/build |
| `README.md` | 项目说明: 一句话介绍/技术栈/快速启动/文档索引 |
